{
  "mcpServers": {
    "project-commands": {
      "command": "node",
      "args": [".claude/server.js"],
      "env": {}
    }
  },
  "slashCommands": {
    "sync": {
      "description": "Sync project changes to git",
      "handler": "git add . && git commit -m \"sync: update with latest changes - 🤖 Generated with Claude Code\\n\\nCo-Authored-By: Claude <noreply@anthropic.com>\" && git push && echo \"✅ 프로젝트 동기화 완료\""
    },
    "commit": {
      "description": "Commit and push changes",
      "handler": "git add . && git commit -m \"feat: automated commit - 🤖 Generated with Claude Code\\n\\nCo-Authored-By: Claude <noreply@anthropic.com>\" && git push"
    },
    "dev": {
      "description": "Run the multi-agent QA system",
      "handler": "npm run dev"
    },
    "status": {
      "description": "Check system status",
      "handler": "echo \"🔍 Git Status:\" && git status --short && echo \"\" && echo \"🔧 TypeScript Check:\" && npm run typecheck"
    },
    "inspect": {
      "description": "Inspect system health and compliance",
      "handler": "npm run status"
    }
  }
}
